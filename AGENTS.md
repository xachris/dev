# AGENTS.md — School Platform 接手与开发约束

本文件面向后续 AI 编码代理、开发者和供应商。

**开始开发前必须先理解已有产品目标和架构决策。不要把本仓库当作可以按个人偏好重新选技术栈的空白项目。**

## 1. 必须先读

按以下顺序阅读：

1. `README.md`
2. `docs/PRODUCT_PHILOSOPHY.md`
3. `docs/decisions/README.md` 以及所有状态为 `Accepted` 的 ADR
4. `docs/ARCHITECTURE.md`
5. `docs/DEVELOPMENT_PLAN.md`
6. `docs/VERIFICATION_STANDARD.md`
7. `docs/worklog/README.md` 和最近阶段日志

如果任务涉及权限，再读 `docs/PERMISSION_MODEL.md`；涉及数据结构，再读 `docs/DATA_MODEL.md`；涉及正式环境，再读 `docs/SECURITY_BASELINE.md`。

## 2. 不得静默推翻已有架构决策

已有架构决定可以修改，但**不能因为模型偏好、框架流行度、个人习惯或“更现代”而直接替换**。

如果一个 `Accepted` ADR 已经覆盖当前问题，先遵守它。

如果现实条件已经变化、确实需要推翻，应新增 ADR，明确：

- 原方案当时为什么合理；
- 哪个真实条件已经变化；
- 当前方案出现了什么被验证的问题；
- 新方案带来什么可衡量业务收益；
- 迁移与回滚成本；
- 安全、权限和业务语义如何保持不退化；
- 新方案如何通过 Pilot 证明优于旧方案。

## 3. 当前前端架构决定

当前默认：

```text
Django + PostgreSQL + Django Templates + CSS + 少量 JavaScript
```

详见：`docs/decisions/ADR-001-frontend-strategy.md`。

**不要在没有新 ADR 和真实业务证据的情况下，把现有 UI 全面迁移到 React / Next.js。**

当前原因包括：

- 登录、Policy、Service、View、Template 到浏览器的闭环已经真实实现并测试；
- 当前核心页面以表格、表单、审核、状态流为主，并不天然需要 SPA；
- 全面 React 化会新增 API、前端鉴权、CORS、Node 构建链、客户端状态管理和第二套测试；
- 当前最重要的验证是教师 / 班主任的真实业务效率，而不是前端框架本身；
- 项目采用 Complexity on demand：真实交互复杂度出现后再增加前端复杂度。

未来可以重新评估 React，但必须按 ADR-001 的触发条件和迁移规则执行。

## 4. 不得破坏的业务原则

无论使用什么技术，必须继续遵守：

- No Dead Data：没有消费者、不会触发行动或决策的数据不要提前收集；
- 一份事实只维护一次，多处消费；
- 正常状态尽量自动推导，人主要处理异常和判断；
- 权限来自真实身份和关系，不维护第二份人工权限名单；
- 学生 / 家长 / 教师 / 班主任 / 管理员内容边界由服务器端控制；
- 前端隐藏按钮不等于权限；
- 技术管理员默认不等于教育内容读取者；
- 多学校隔离必须始终在服务器端验证；
- AI 不能推断收件人、学生关系或授权关系；
- 发布报告是业务状态变化，不等于发送邮件；
- 核心业务流程不能依赖 AI 才能运行。

## 5. 开发方式

每一段开发都需要：

1. 小范围、可解释的变更；
2. 自动化功能核验；
3. 负向权限 / 异常路径核验（适用时）；
4. 业务意义核验；
5. 更新开发日志；
6. 明确收益和遗留风险。

“测试全绿”不等于阶段完成。

还必须回答：

- 这段代码减少了什么人工？
- 避免了什么重复？
- 提高了什么准确性或用户体验？
- 是否新增了死数据或第二份真相？
- 是否增加了不值得的维护复杂度？
- 当前风险是什么？

## 6. 工作日志要求

完成一个有意义的开发切片后，更新 `docs/worklog/`。

日志至少记录：

- 做了什么；
- 对业务的可能影响；
- 带来的好处；
- 遗留风险；
- 功能核验结果；
- 业务意义核验结果。

不要只写“修改了哪些文件”。

## 7. 当前产品阶段

当前首个完整闭环是学习报告系统，并已经有基于 Django Templates 的 Pilot UI。

近期目标不是重新设计技术栈，而是：

1. 让线上 Pilot 稳定可访问；
2. 使用纯虚拟数据进行真人浏览器测试；
3. 测量教师连续录入、点击次数、完成时间和班主任审核效率；
4. 根据真实使用阻力决定下一轮 UX 和架构改进。

在 Event / Audit / Notification、正式安全评估、备份恢复等完成之前，不得把虚拟数据 Pilot 擅自升级为真实学生数据生产环境。
